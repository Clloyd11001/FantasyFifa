<% if @bracket.present? %>
  <h2>Bracket Tree</h2>
  <div class="bracket-tree">
    puts "Bracket Data: #{bracket.inspect}"
    <%= render_bracket(@bracket) %>
  </div>
<% else %>
  <p>No bracket data available.</p>
<% end %>
