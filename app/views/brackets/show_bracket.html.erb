<!-- Assuming @bracket contains the bracket data -->

<% if @bracket.present? %>
  <h2>Bracket Tree</h2>
  <div class="bracket-tree">
    <%= render_bracket(@bracket.bracket_tree) %>
  </div>
<% else %>
  <p>No bracket data available.</p>
<% end %>
